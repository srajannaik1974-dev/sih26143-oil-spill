# ── Core API framework ─────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0        # ASGI server with websocket + reload support
python-multipart>=0.0.9          # required for FastAPI multipart/form-data uploads
python-dotenv>=1.0.0             # reads .env file into os.environ

# ── Streamlit demo app ─────────────────────────────────────────────────────────
streamlit>=1.35.0                # Streamlit web UI for SIH demo
huggingface_hub>=0.23.0         # download model checkpoint from Hugging Face Hub
matplotlib>=3.8.0               # 4-panel visualization in Streamlit app
tensorflow>=2.15.0              # Keras backend for Streamlit inference
keras>=3.0.0                    # keras.saving.load_model for .keras checkpoint
opencv-python-headless>=4.9.0   # bilinear resize for SAR preprocessing (headless = no GUI deps)

# ── Response encoding ──────────────────────────────────────────────────────────
Pillow>=10.0.0                   # PNG-encode mask arrays for base64 API response

# ── ML layer ─────────────────────────────────────────────────────────────────────
torch>=2.0.0                    # used by FastAPI /api/ml/predict (PyTorch backend)
numpy>=1.24.0
rasterio>=1.3.0

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0           # async test support
httpx>=0.27.0                    # async test client for FastAPI (TestClient)
